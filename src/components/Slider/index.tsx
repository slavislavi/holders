import React from 'react';
import {Text, View} from 'react-native';
import {styles} from './styles';

export const Slider = () => {
  return (
    <View style={styles.container}>
      <Text>slider clipboard</Text>
    </View>
  );
};
